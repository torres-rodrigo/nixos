# 117 compiled cursors

This directory is reserved for final Linux Xcursor binaries.

The source SVGs, rendered PNGs, and `xcursorgen` config files have already
been prepared. Install `xcursorgen` or `ctgen` before compiling the final
cursor files and alias symlinks into this directory.
